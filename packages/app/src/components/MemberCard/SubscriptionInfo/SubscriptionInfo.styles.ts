import styled from "styled-components/native";

export const NoSubscriptionText = styled.Text`
  font-weight: normal;
  font-size: 17px;
  line-height: 22px;
  color: #f11c00;
`;

export const SubscriptionExpiredText = styled.Text`
  font-weight: normal;
  font-size: 17px;
  line-height: 22px;
  color: #f11c00;
`;

export const FieldValue = styled.Text`
  font-weight: normal;
  font-size: 17px;
  line-height: 22px;
  color: #000000;
`;
